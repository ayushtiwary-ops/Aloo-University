/**
 * Date utility functions.
 * Phase 2 implements age and date-range calculations.
 * Signatures are final.
 */
export const dateUtils = {
  /** @param {string} iso @returns {number|null} */
  getAge: (iso) => null,

  /** @param {string} iso @param {number} minAge @returns {boolean} */
  isMinimumAge: (iso, minAge) => false,

  /** @param {string} iso @returns {string} — DD/MM/YYYY */
  toDisplayDate: (iso) => iso,
};
